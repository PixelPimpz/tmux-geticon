#!/usr/bin/env bash
usage="${0##*/}"
printf '%s\n' $usage
tmux setenv '@PLUG_ROOT' "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
#$PLUG_ROOT="$(tmux display -p "#{@PLUG_ROOT}")"
#$ICONS="$(tmux display -p "#{@LIB_ICON}")"
#tmux display -p "$usage"
#$PLUG_ROOT/scripts/tmux-geticon.sh
#tmux bind M-m usage

run "$PLUG_ROOT/tmux-geticon"
